[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
# Set dummy DATABASE_URL for prisma postinstall script
cmds = ["DATABASE_URL=postgresql://x:x@localhost:5432/x npm ci"]

[phases.build]
cmds = []

[start]
cmd = "npx prisma generate && npx prisma db push && node src/index.js"

